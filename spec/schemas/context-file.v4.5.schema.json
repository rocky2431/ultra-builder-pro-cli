{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ultra-builder-pro.dev/spec/context-file.v4.5.schema.json",
  "title": "context-file projection (v4.5)",
  "description": "Schema for the parsed representation of .ultra/.runtime/projections/contexts/task-*.md. The file is regenerated by the projector from state.db and remains checkout-local. Authority boundary: docs/ARTIFACT-AUTHORITY.md.",
  "type": "object",
  "required": ["header", "body"],
  "additionalProperties": false,
  "properties": {
    "header": {
      "type": "object",
      "required": ["task_id", "title", "status", "schema_version"],
      "additionalProperties": false,
      "properties": {
        "task_id":        { "type": "string" },
        "title":          { "type": "string" },
        "status": {
          "type": "string",
          "enum": ["pending", "in_progress", "completed", "blocked", "expanded"],
          "x-derived": true,
          "description": "DERIVED from state.db tasks.status; written by projector, not by humans"
        },
        "priority":       { "type": "string", "minLength": 1, "maxLength": 80 },
        "type":           { "type": "string", "minLength": 1, "maxLength": 80 },
        "session_id":     { "type": "string", "x-derived": true },
        "schema_version": { "const": "4.5" },
        "generated_at":   { "type": "string", "format": "date-time" }
      }
    },
    "body": {
      "type": "object",
      "required": ["sections"],
      "additionalProperties": false,
      "properties": {
        "sections": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["heading", "content"],
            "additionalProperties": false,
            "properties": {
              "heading": {
                "type": "string",
                "not": { "pattern": "^[Ss]tatus" },
                "description": "body sections cannot be 'Status' — that lives only in header (D17)"
              },
              "content": { "type": "string" }
            }
          }
        }
      }
    }
  }
}
