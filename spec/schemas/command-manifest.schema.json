{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ultra-builder-pro.dev/spec/command-manifest.schema.json",
  "title": "Ultra command launcher frontmatter",
  "description": "Portable source contract for a thin command launcher. Runtime metadata and workflow procedure belong outside the command file.",
  "type": "object",
  "required": ["description", "workflow-ref"],
  "additionalProperties": false,
  "properties": {
    "description": {
      "type": "string",
      "minLength": 10,
      "maxLength": 240,
      "description": "Concise user-visible action description."
    },
    "argument-hint": {
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" }, "minItems": 1 }
      ],
      "description": "Optional host launcher hint; it does not define argument semantics."
    },
    "workflow-ref": {
      "type": "string",
      "pattern": "^@skills/[a-z][a-z0-9\\-]*/SKILL\\.md$",
      "description": "The single Skill that owns this workflow."
    }
  }
}
