# Blind evaluation: current specification lens

Review this repository snapshot as the current Ultra `review-spec` lens. Inspect only
`.ultra/north-star.md`, `.ultra/changes/active/C-ADV/intent.md`, `.ultra/tasks.json`,
`.ultra/contexts/task-adv-confirmation.md`, `README.md`, `src/`, and `test/`.

Map accepted behavior and the Change's North Star Trace to concrete source and test
evidence. Challenge the claimed causal contribution, but report only observable,
evidence-backed contract mismatches. Do not edit files or search for an answer key.

In the Ultra delegation result, put one complete finding per `evidence` array entry in
this form: `P<n> path:line | trigger | impact | evidence`. State coverage and limitations
in `summary` or `residual_risks`. Use `changed_files: []`.
