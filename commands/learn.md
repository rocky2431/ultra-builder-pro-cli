---
description: Capture one verified reusable workflow as a portable user skill after explicit approval
argument-hint: "[skill-name]"
workflow-ref: "@skills/learn/SKILL.md"
---

# Capture a reusable skill

Read and follow `@skills/learn/SKILL.md` with `$ARGUMENTS` as optional naming guidance.
Treat the referenced skill as the only workflow definition.
