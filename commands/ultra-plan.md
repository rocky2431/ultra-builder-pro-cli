---
description: Convert validated requirements into dependency-valid Ultra tasks with bounded context and exact verification
argument-hint: "[scope]"
workflow-ref: "@skills/ultra-plan/SKILL.md"
---

# Plan executable slices

Read and follow `@skills/ultra-plan/SKILL.md`, using `$ARGUMENTS` to select the requested scope.
Treat the referenced skill as the only workflow definition.
